# Optional extra dependencies for src/data/generate_synthetic_defects.py
# (ControlNet + Stable Diffusion inpainting). Not needed for the rest of
# the pipeline -- install only if you want to generate synthetic defects:
#   pip install -r requirement.txt -r requirement-synthetic.txt
diffusers
transformers
accelerate
